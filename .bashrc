stty erase ^H

PS1='[\u@\h \W]\$ '

export EDITOR=emacs

alias '...'='cd ../../'
alias ls='ls --color=auto'
alias ll='ls -la'
alias vi=nvim
alias vim=nvim

#eval "$(opam env)"
#eval "$(direnv hook bash)"
